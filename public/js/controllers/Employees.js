define(['./module'], function(controllers){
    "use strict";

    controllers.controller('EmployeesController',['$scope', function($scope) {
        var self = this;

        alert('Привет!');
    }]);
});